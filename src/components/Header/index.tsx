import React from 'react';

import {Box, Heading} from 'native-base';

const Header = () => {
  return (
    <Box width="full">
      <Heading size="xl">Yesterday</Heading>
    </Box>
  );
};

export default Header;
